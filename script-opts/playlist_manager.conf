# playlist_manager.conf — options for scripts/playlist_manager.lua

# Resolve human-readable titles for URL playlist entries via yt-dlp.
# Set to no to disable background yt-dlp fetches (faster startup, titles show as raw URLs).
resolve_url_titles=yes
